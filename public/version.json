{
  "version": "1.6.2",
  "changes": [
    "Fixed password storage to use bcrypt only and removed plain-text password handling.",
    "Admin accounts now seed from .env when available and require a password change when the fallback is used.",
    "Inventory seeding now avoids duplicate barcodes during startup on both new and existing databases.",
    "Non-admin inventory edits are now restricted to status-only updates.",
    "Server-provided dashboard and eLoad content now renders safely without unsafe innerHTML."
  ]
}
