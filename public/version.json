{
  "version": "1.9.1",
  "changes": [
    "Removed automatic demo seeding for inventory, suppliers, categories, sales, and sale items during startup.",
    "Fresh installs now initialize only required system records such as the admin account, roles, permissions, store settings, and e-load defaults.",
    "Deleted inventory items are no longer recreated automatically on startup.",
    "Startup no longer depends on sample products, which prevents seedSales-related crashes on empty production databases."
  ]
}
